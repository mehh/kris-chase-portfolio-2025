echo "🔍 Running pre-commit checks..."
echo ""

# Run linting
# NOTE: Temporarily disabled due to Next.js 16 ESLint configuration issue
# Uncomment when Next.js lint issue is resolved
# echo "📝 Running ESLint..."
# npm run lint || {
#   echo "❌ ESLint failed. Please fix the errors before committing."
#   exit 1
# }
echo "⏭️  ESLint check skipped (Next.js 16 configuration issue)"

# Run type checking
echo "🔷 Running TypeScript type check..."
npm run type-check || {
  echo "❌ TypeScript type check failed. Please fix the errors before committing."
  exit 1
}

# Run build to ensure everything compiles
echo "🏗️  Running build..."
npm run build || {
  echo "❌ Build failed. Please fix the errors before committing."
  exit 1
}

echo ""
echo "✅ All pre-commit checks passed!"