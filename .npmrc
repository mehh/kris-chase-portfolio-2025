# Ensure Tailwind uses WASM fallback instead of native bindings
# This prevents build failures on Vercel and other CI environments
# NAPI_RS_FORCE_WASI is the actual env var that @tailwindcss/oxide checks
NAPI_RS_FORCE_WASI=1
TAILWIND_DISABLE_NATIVE=1

