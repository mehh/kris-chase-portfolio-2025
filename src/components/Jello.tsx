"use client";

import React from "react";

export default function Jello() {
  // Temporarily disabled due to build issues after shadcn installation
  // This component will be re-enabled once the WebGL setup is fixed
  return null;
}
